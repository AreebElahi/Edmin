'use client';

// Legacy placeholder component — main ticket management is handled by /dashboard/admin/tickets/page.tsx
// This component is kept for reference only.
import React from 'react';

export default function TicketInbox() {
  return (
    <div className="p-6">
      <p className="text-text-secondary text-sm">Legacy inbox. Use the Tickets page instead.</p>
    </div>
  );
}
